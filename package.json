{
  "name": "noaqh-dev",
  "version": "0.1.4",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "noaqh-dev": "./bin/noaqh-dev"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "generate-prompt": "bun run script/generate-prompt.ts",
    "install-prompts": "bun run generate-prompt && bun run script/install-codex-prompts.ts",
    "update": "bun remove -g noaqh-dev && bun install -g github:noaqh-corp/dev && bunx noaqh-dev install-prompts && bunx noaqh-dev install-mcp",
    "cli": "bun run src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@biomejs/biome": "^2.3.9",
    "@modelcontextprotocol/sdk": "^1.20.1",
    "ecto": "^4.6.0",
    "express": "^5.1.0",
    "fastmcp": "^3.20.0",
    "zod": "3"
  }
}
